# send

> API_LEVEL `2.0` 开始支持，API 兼容性请参考 [API_LEVEL](../../../../guides/framework/device/compatibility.md)。

发送消息，`data` 待发送数据，`size` 待发送数据长度。

## 类型

```ts
function send(data: object, size: number): void
```

## 代码示例

```js
import { send } from '@zos/ble'

// ...
```
