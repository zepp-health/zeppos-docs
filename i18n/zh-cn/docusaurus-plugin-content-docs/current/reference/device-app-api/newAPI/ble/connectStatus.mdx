# connectStatus

> API_LEVEL `2.0` 开始支持，API 兼容性请参考 [API_LEVEL](../../../../guides/framework/device/compatibility.md)。

查询连接状态，`true` 表示连接，`false` 表示未连接。

## 类型

```ts
function connectStatus(): boolean
```

## 代码示例

```js
import { connectStatus } from '@zos/ble'

// ...
```
