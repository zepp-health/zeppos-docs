---
title: updateLayout()
sidebar_label: updateLayout
---

> Supported from API_LEVEL `4.0`. For API compatibility, please refer to [API_LEVEL](../../../../guides/framework/device/compatibility.md).

Used to re-render the view after modifying the widget tree.

## Type

```ts
() => void
```

## Example

```js
import { createWidget, widget, updateLayout } from '@zos/ui'

const container = createWidget(widget.VIRTUAL_CONTAINER)
const button = createWidget(widget.BUTTON)

// Add child node
container.addLayoutChild(button)

// Update layout and re-render the view
updateLayout()
```

## Related References
- [Widget layout properties for Flex layout](../../../../guides/framework/device/layout.md)
